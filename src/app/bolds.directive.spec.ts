import { BoldsDirective } from './bolds.directive';

describe('BoldsDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldsDirective();
    expect(directive).toBeTruthy();
  });
});
